const Page = () => {
  return (
    <div>DASHBOARD page</div>
  )
}

export default Page
// to make new routes you just have to create a folder inside of app folder. the folder name specifies the route. So if you name the folder dashboard then the route is gonna be /dashboard. And also you need to create a file always name as page.tsx inside of the folder. Creating page.tsx is mandatory.