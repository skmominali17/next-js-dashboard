import { Inter } from 'next/font/google';
import { Lusitana } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });

export const lustaina = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
});